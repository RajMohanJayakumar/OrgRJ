/**
 * Gateway Configuration
 *
 * This file defines the routing configuration for the universal gateway.
 * Configuration is now generated from the routes map for consistency.
 */

import { generateGatewayConfig } from './routes-map.js';

// Generate configuration from routes map
export default generateGatewayConfig();
